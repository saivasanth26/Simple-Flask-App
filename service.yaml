apiVersion: v1
kind: Service
metadata:
  labels:
    app: flaskmonitor
  name: flaskmonitor
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: flaskmonitor
